{
  "title": "HomeKit Guard",
  "config": {
    "step": {
      "user": {
        "title": "Set up HomeKit Guard",
        "description": "Install HomeKit Guard to control whether selected HomeKit Bridge commands are blocked."
      }
    },
    "abort": {
      "already_configured": "HomeKit Guard is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "HomeKit Guard options",
        "description": "Choose Home Assistant service calls to block or allow. If only allowed calls are configured, all other HomeKit calls are blocked. If only blocked calls are configured, only those calls are blocked.",
        "data": {
          "blocked_services": "Blocked service calls",
          "allowed_services": "Allowed service calls"
        }
      }
    }
  },
  "entity": {
    "switch": {
      "status": {
        "name": "Status"
      }
    }
  }
}
